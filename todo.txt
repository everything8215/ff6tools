+-------------------------+
| ff6tools todo/wish list |
+-------------------------+

general
-------
 * patch/asm support
 * add an option to switch between hex and decimal
 * manual relocation
 * reserve rom ranges (remove free space for patches or compatibility with other editors)
 * make things load faster! (load event script in parallel ???)
 * log rom map
 * copy and paste
 * paste-able script text
 * disable automatic snes header update
 * import/export binary data
 * import custom tbl file
 * add a multi-bank option for assemblies that can span multiple banks
 * emulate parallax scrolling when screen mask is on
 * improve fragmented assemblies
 * organize source/definition files in github folder
 * add delegates for objects to intercept assembly methods
 * import/export palettes
 * store palettes as Uint8Array instead of Uint32Array

gui
---
 * custom property controls (entrance trigger destination, rendered text, colosseum battle, fill tile)
 * grid controls (for element/status, esper/natural magic)
 * tile mask layers for map editor (ff1, ff2, ff5)
 * add directional passability masks for ff6
 * legend for tile mask colors
 * allow multiple selection, cut/copy/paste in script list
 * make tileset zoom in when sidebar is expanded
 * progress bar
 * show rom title in top bar
 * script list/trigger double click functionality
 * navigator labels should update automatically
 * find a better way to auto-populate battle names
 * array insert/remove
 * arrayPath/objectPath in navigator

game specific
-------------
 * add english string tables for ff6 steam version
 * world of ruin gets corrupted in roms expanded by ff3usme
 * ff5 monster ai
 * ff5 advance (battles, ff5j, ff5e)
 * can't expand/relocate ff6 advance text
 * battle vram viewer for ff5
 * draw enemy characters in ff4 advance
 * ff4 advance japanese version
 * ff4-sfc monster/summon graphics (fragmented)
 * ff5-sfc monster graphics (set format automatically)
 * import ff6 spritesheets

wish list
---------
 * battle animation editor
 * cinematic editor
 * tile layout editor
 * music editor
 * ff1 and ff2 nes/wsc/gba/psp
 * ff3 nes
 * ff4 wsc and psp
 * psx versions
